<h1>Список пользователей</h1>
<div class="container">
    {{#each projects}}
    <div class="project-preview">
        <div>
            <img src="{{this.image}}">
        </div>
        <div>{{this.name}}</div>
        <div>{{this.description}}</div>
    </div>
    {{/each}}
</div>